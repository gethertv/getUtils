jdk:
  - openjdk17

before_install: []

install:
  - mvn install:install-file -DgroupId=dev.gether -DartifactId=getUtils -Dversion=1.0 -Dpackaging=jar -DgeneratePom=true

script:
  - mvn clean install